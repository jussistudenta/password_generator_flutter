name: password_generator_app
description: Password generator app (coding experiment)

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.0.6

dependencies:
  flutter:
    sdk: flutter
    
  provider: ^6.0.0
  password_generator: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true